# Completed work items — append only, newest first

- id: nav-access-flags-config-page
  date: 2026-05-18
  title: "Fix: perf/a11y/contract/visual nav items invisible on /config page"
  outcome: >
    config/__init__.py config_page() built its own context dict that only set
    vapt_access; the 4 new module flags were undefined (falsy), hiding nav items
    when the user navigated to /config. Fixed with a local _has_flag(flag) helper
    covering all 5 flags. Nav items now consistent across all pages.

- id: test-plans-empty-modules-ux
  date: 2026-05-18
  title: "Test Plans: empty-modules empty state UX fix and API error surfacing"
  outcome: >
    Empty state in Scope tab now shows the expected file path
    (products/{product}/tests/{domain}/) and test_*.py pattern so users know
    where to add scripts. loadAvailableModules now triggers sfToast on API
    failure (!r.ok or network error) instead of silently leaving the list blank.
    Added scopeLoadError flag to distinguish API failure from legitimately empty
    directory. scopeLoadError reset on plan change and at start of each load.

- id: config-module-feature-flags
  date: 2026-05-18
  title: "Config: Perf/A11y/Contract/Visual feature flag toggles in product settings"
  outcome: >
    Added On/Off toggle buttons for all 4 new modules in Configurations → Products.
    Backend: _set_flag() helper + 4 POST endpoints (set-perf, set-a11y, set-contract,
    set-visual) writing to config.yaml. Template: coloured pill buttons matching VAPT
    toggle pattern (blue/sky/emerald/pink). Fixed missing module enable path that caused
    "No X-enabled products" message on all 4 new module pages.

- id: four-new-testing-modules
  date: 2026-05-18
  title: "4 new modules: Performance Testing, Accessibility Testing, API Contract Validation, Visual Regression"
  outcome: >
    All 4 modules follow VAPT pattern: FileLock JSON storage, FastAPI router,
    Alpine.js dashboard (modals + polling), WeasyPrint PDF report, nav items in sidebar.
    Feature flags per product config. _module_access() helper in pages.py.
    Performance: httpx+asyncio load engine; profiles (load/stress/spike/soak);
    VUs/duration/ramp-up/endpoints; p50/p75/p95/p99/throughput/error-rate metrics;
    configurable thresholds; PDF report. Accessibility: Playwright + axe-core CDN;
    WCAG 2.1 A/AA/AAA; per-page violations by impact (critical/serious/moderate/minor);
    multi-scan history; PDF report. API Contract: OpenAPI spec from URL or file path;
    validates status codes + jsonschema per endpoint; skips required path-param routes;
    PDF report. Visual Regression: Playwright full-page screenshots; Pillow pixel diff
    via ImageChops.difference; configurable threshold %; baseline management per page;
    comparison scan shows diff_pct per page; PDF report. Pillow>=10.0.0 added to
    requirements.txt. Files: core/perf_manager.py, core/a11y_manager.py,
    core/contract_manager.py, core/visual_manager.py; 4 routers; 4 main HTML templates;
    4 PDF report templates. 20 total dashboard pages/routers.

- id: vapt-apk-static-analysis
  date: 2026-05-18
  title: "VAPT: APK static analysis + infra/mobile scope inputs + scan delete + per-type reports/certs"
  outcome: >
    Per-scan-type reports/certs: check_certifiable/issue_certificate filter by scan_type;
    certs in eng["certificates"][scan_type]; download endpoints accept scan_type param;
    scan type toggle moved above sub-tabs. Scan delete: VaptManager.delete_scan() +
    DELETE endpoint; admin Delete button on scan cards. Infra targets: scope.infra_targets[],
    textarea in scope form, VAPT_INFRA_TARGETS env var to subprocess; conftest
    vapt_infra_targets fixture. Mobile app path: scope.mobile_app_path, VAPT_MOBILE_APP_PATH
    env var; vapt_mobile_app_path fixture (Path|None). APK static analysis: 2 new generated
    files — test_vapt_mobile_apk_manifest.py (androguard: debuggable, allowBackup,
    usesCleartextTraffic, exported components, taskAffinity) and test_vapt_mobile_apk_static.py
    (pure Python: private keys, hardcoded secrets/http URLs in assets, weak crypto in DEX;
    androguard: DEX string pool secrets, cert pinning, Log calls). androguard>=3.3.5 added
    to requirements.txt. All APK tests skip gracefully without APK or androguard.

- id: docs-tc-create-delete-ux
  date: 2026-05-18
  title: "Docs: TC create success toast + sfConfirm delete + Alpine timing fix"
  outcome: >
    tc_create redirect appends ?created=TC_ID; docsPage.init() shows sfToast
    then cleans param via history.replaceState. Delete inline confirm replaced
    with sfConfirm modal + hidden x-ref form triggered via htmx.trigger.
    tcCreateForm moved to docs.html block scripts (page-load scope) — fixes
    Alpine MutationObserver race where function was undefined on first HTMX swap.

- id: coverage-gap-pipeline-orangehrm
  date: 2026-05-18
  title: "Coverage gap pipeline: orangehrm 67% → 100% doc coverage + pipeline hardening"
  outcome: >
    CoverageGapAgent run across api/web/mobile: 96 untested KB scenarios logged to activity.
    Full pipeline (DocGen→DocReview→ScriptGen→ScriptReview) for 4 undocumented scripts:
    security_api.md (OH-API-029..039), security_web.md (OH-WEB-083..125),
    leave.md (OH-WEB-124..128), login_mobile.md (OH-MOB-017..026).
    Pipeline bugs fixed: normalizer regex now excludes not_automated + not_automatable;
    max_tokens 3000→5000; mobile CRITICAL — no base_url in mobile POM constructors.
    Credential hardcoding eliminated: api convention rewritten with {product}_client /
    {product}_api_base_url / {product}_credentials patterns; two NEVER rules added to
    prompt_templates.py. orangehrm_ess_credentials fixture + env_qa.yaml ess keys added.

- id: docs-manual-tc-creation
  date: 2026-05-18
  title: "Docs: manual test case creation from the dashboard"
  outcome: >
    Add Test Case button on Docs page loads a form (HTMX → #tc-viewer).
    TC ID auto-generated from highest existing ID in product+domain.
    Feature dropdown shows existing modules; "Add new module" reveals text input.
    Script restricted to actual test_*.py files; warns if already linked elsewhere.
    Owner dropdown from config.yaml users. On save: appends to existing .md or
    creates new file with full header. Duplicate ID rejected. HX-Redirect refreshes
    left panel. Fixed Alpine | safe bug (raw JSON in double-quoted attribute) and
    moved <script> to top of partial (before MutationObserver fires).

- id: quality-vapt-exclusion-coverage-signal
  date: 2026-05-18
  title: "Quality metrics: exclude VAPT tests; low-coverage doc-gen signal"
  outcome: >
    quality.py: _EXCLUDED_DOMAINS = {"vapt"} applied in _script_features(),
    _scripts_by_domain(), _count_test_functions() — VAPT template-generated tests
    no longer inflate script count or drag down doc coverage.
    _undocumented_with_domain(product) returns [{feature, domain}] for scripts
    lacking a matching test case doc. per-product data gains undocumented_scripts list.
    quality.html: health signals section adds low-coverage card per product when
    doc_coverage < 70% — collapsible list of undocumented scripts, each with a
    Generate Doc button (POST /api/pipeline/trigger, skip_script=true, Alpine state
    machine idle→pending→done/error, sfToast feedback).

- id: tc-doc-section-spacing
  date: 2026-05-18
  title: "TC doc section spacing: blank lines before Pre-conditions/Steps/Expected Result"
  outcome: >
    prompt_templates.py: web + API format examples now have blank lines between every section.
    orchestrator.py _clean_doc(): _TC_HEADERS regex (pattern: **<keyword>[^*]*\*\*) ensures
    blank line before each bold section header on every pipeline run; also strips inline ```
    fences. 23 existing docs reformatted in one pass. Bug caught: colon is inside bold
    markers (**Pre-conditions:**), not after — fixed regex accordingly.

- id: vapt-exhaustive-suite-template-viewer
  date: 2026-05-18
  title: "VAPT: exhaustive A01-A10 test suite, template viewer, dedup product filter"
  outcome: >
    core/vapt_test_generator.py: 9 test files / 31 tests. New: test_vapt_injection
    (A03 — SQL/XSS/path traversal/SSTI), test_vapt_session (A07 — cookie flags),
    test_vapt_web (A01/A05 — clickjacking/open redirect/referrer/HTML comments),
    test_vapt_ssrf (A10 — IMDS/RFC-1918 probes). OWASP inference: clickjack A03→A05,
    new keywords path_traversal/httponly/samesite/dns_rebinding. vapt.py: test-templates
    endpoint. vapt.html: collapsible template viewer (file tabs + OWASP context + code).
    pages.py: vapt_page() calls vapt._vapt_products() — no more duplicated filter logic.
    RP: 25 passed, 3 skipped, 3 xfailed.

- id: script-fn-id-normalizer
  date: 2026-05-18
  title: "Orchestrator: _normalize_script_fn_ids() — code-level TC ID backstop for scripts"
  outcome: >
    orchestrator.py: _normalize_script_fn_ids(script_path, doc_path, tc_prefix) runs after
    _generate_script() and before _review_script(). Extracts TC IDs from doc index table
    (non-not_automatable rows), matches to test function declarations in order, renames any
    function with wrong embedded ID (test_OH_API_001_x → test_OH_API_014_x) or no ID prefix.
    Skips silently when count mismatch (needs human review). Mirrors _normalize_tc_ids() for docs.

- id: quality-dashboard-metrics
  date: 2026-05-18
  title: "Quality dashboard: real pass rate, automated test count, execution stats, risk score"
  outcome: >
    quality.py: pass rate now from test_runs table (actual pytest results), not activity_log.
    Added _count_test_functions() (def test_ grep per product), _run_stats_from_db() (aggregates
    total/passed/failed/duration), _daily_pass_rate_from_runs() (7-day daily trend from test_runs),
    _risk_score() (composite 0–100: pass gap 50pt + quarantine rate 20pt + doc gap 20pt + flaky 10pt).
    quality.html: expanded from 5 to 6 summary cards (Automated Tests, Pass Rate, Executions,
    Doc Coverage, Quarantined, Risk Score). Each card has Alpine ⓘ tooltip with formula.
    Per-product table: auto test function count, real pass rate, execution breakdown (passed✓/failed✗),
    risk badge (Low/Med/High + score). Live data: 169 test functions, 44% pass rate, 191 executions,
    risk 43 (Medium).

- id: timesheets-pipeline-fixes
  date: 2026-05-18
  title: "Timesheets pipeline: ScriptReview KeyError fix, orchestrator fixes, correct test scripts"
  outcome: >
    script_review_agent.py: escaped {{"positive","negative","edge"}} in _REWRITE_PROMPT — str.format()
    was raising KeyError silently, causing all ScriptReview passes to be skipped.
    orchestrator.py: DocReview now always runs with --doc flag; --doc path resolved absolute;
    default doc-model changed to qwen2.5-coder:14b-instruct-q4_K_M.
    tests/api/test_Timesheets.py: rewritten OH-API-014..028 with real assertions and orangehrm_client.
    tests/web/test_Timesheets.py: completed all 11 TCs OH-WEB-072..082.

- id: vapt-standard-test-suite
  date: 2026-05-18
  title: "VAPT standard test suite — per-product generation on enable"
  outcome: >
    core/vapt_test_generator.py: VaptTestGenerator generates products/<product>/tests/vapt/
    with conftest.py (auto-detects base_url/api_token from env_*.yaml) and 5 test files:
    test_vapt_auth (A07), test_vapt_headers (A05), test_vapt_access_control (A01),
    test_vapt_crypto (A02), test_vapt_rate_limiting (A04). 17 tests total.
    Rate-limiting tests use xfail (advisory), HTTPS tests use skip for local envs.
    _products.py: auto-generates on first VAPT enable. vapt.py: test-info + generate-tests
    endpoints. vapt.html: test suite status card + Regenerate button in Scans tab.
    reportportal vapt suite: 13 passed, 2 skipped, 2 xfailed against live RP instance.

- id: vapt-security-review-fixes
  date: 2026-05-18
  title: "VAPT security review: autoescape HTML reports + redundant auth dependency"
  outcome: >
    dashboard/routers/vapt.py: _render_template() now uses jinja2 Environment with
    autoescape=True and FileSystemLoader — prevents XSS from test-failure evidence strings
    in the HTML report preview endpoint. issue_certificate consolidated to single
    Depends(_require_admin) since _require_admin already wraps require_user.

- id: doc-gen-robustness-vapt-certifiability
  date: 2026-05-18
  title: "Doc generation: strip code fences + TC_ID placeholder handling; VAPT certifiability fix"
  outcome: >
    orchestrator.py: _clean_doc() strips ``` code-fence wrappers models add around output
    before normalization runs. _normalize_tc_ids() now replaces literal TC_ID placeholders
    (models copying prompt template verbatim) in addition to renumbering PREFIX-NNN IDs.
    vapt_manager.py: check_certifiable() now requires at least one completed scan
    (was checking for any findings, which could be satisfied without a completed scan run).
    ReportPortal security scan: 5/5 auth tests passed against live RP instance.

- id: tc-id-generation-hardening
  date: 2026-05-17
  title: "TC ID generation hardening — normalizer, stronger prompt, increments string guard"
  outcome: >
    orchestrator.py: _normalize_tc_ids() post-processes every generated doc,
    remapping wrong IDs to the correct sequential range starting at tc_start.
    test_case_doc_kb.py: _build_tc_id_instruction() now shows a concrete
    4-step example sequence and "NEVER start from PREFIX-001" rule.
    kb_loader.py: get_increments_context() handles string 'feature' keys
    (was crashing and skipping DocReview silently on every pipeline run).

- id: doc-id-conflict-fix
  date: 2026-05-17
  title: "TC ID conflict prevention — rulebook, pipeline auto-detection, doc fixes, new docs"
  outcome: >
    conventions.md: added TC ID Rules section with prefix table, 5 uniqueness rules,
    current highest ID snapshot. orchestrator.py: _find_highest_tc_id() auto-detects
    tc_start from existing docs; main() uses it to prevent silent ID collisions.
    test_case_doc_kb.py: CRITICAL warning added to _build_tc_id_instruction() prompt.
    15 existing docs: manual test IDs renumbered away from automated ID ranges
    (OH-WEB 058-071, RB-WEB 012-014). 9 new docs created for undocumented test files
    (security, accessibility, mobile for OH and RB). DocReviewAgent run on all 15 docs.

- id: P2-04+P2-05
  date: 2026-05-17
  title: "ScriptReviewAgent + AI assertion utilities"
  outcome: >
    ai/agents/script_review_agent.py: Post-generation quality gate for pytest scripts.
    Static AST pass detects hardcoded URLs/credentials, time.sleep(), duplicate test
    names, POM base_url issues. LLM rewrite pass fixes weak/missing assertions, missing
    domain markers, missing status-code checks on API tests, and exact-equality checks
    on AI output. Integrated into TestPipelineOrchestrator after _generate_script().
    Registered in __init__, _AGENT_REGISTRY, AGENT_META.
    utils/ai_assertions.py: Hard assertion helpers for non-deterministic AI output —
    assert_confidence_above/below, assert_category_in, assert_text_contains_any/all,
    assert_text_similarity (difflib), assert_response_within, assert_field_present.
    SoftAssertions context manager collects all failures into one combined AssertionError.
    Thresholds (AI_CONFIDENCE_THRESHOLD=0.70, AI_TEXT_SIMILARITY_RATIO=0.75,
    AI_RESPONSE_TIMEOUT_S=30.0) centralised in utils/constants.py.

- id: SQLite-migration+Hetzner+DocReviewAgent
  date: 2026-05-16
  title: "SQLite storage migration, Hetzner deploy, DocReviewAgent, Analyze All"
  outcome: >
    Storage: replaced FileLock+JSON/YAML with SQLite WAL mode (data/sentinelflux.db)
    via core/db.py. All shared state (runs, schedules, approvals, pipeline jobs,
    activity log, quarantine, run history) now in DB. Superseded D-01/D-02.
    pipeline_jobs migrated to SQLite (separate pass, 6e25c71).
    Hetzner: server setup and deployment scripts added (4510522).
    DocReviewAgent (b94dfa3): post-generation quality gate for test case docs.
    Detects batched TC headings, missing mandatory sections, thin TCs (<3 steps).
    Rewrites failing TCs via LLM. Auto-runs after DocGenAgent. Visible in /agents.
    DocGenAgent prompt hardening: mandatory Fields-in-Scope rule, no range headings,
    min-3-steps rule. max_tokens raised (web 3000→6000, API 3000→4000).
    Analyze All bulk endpoint + Re-analyze button for runs page (d8f431e).

- id: D-01-through-D-06+multi-tenant+remote-runner
  date: 2026-05-15
  title: "Debt D-01..D-06 resolution + multi-tenancy + remote runner"
  outcome: >
    D-01: FileLock added to all data writers (later superseded by SQLite migration).
    D-02: QuarantineManager.record_run() trims entries older than 90 days.
    D-03: utils/paths.py created; ROOT imported across all modules.
    D-04: config_router.py split into dashboard/routers/config/ subpackage
    (_helpers, _meta, _users, _products, _assignments, _run_config, _runners).
    D-05: Unit tests added to tests/unit/ for core utilities.
    D-06: _build_ai_client() extended to support openai/anthropic providers.
    Multi-tenancy: per-product run/schedule sharding → later migrated to SQLite.
    Remote runner: pull-based daemon (sentinelflux runner), Bearer token auth,
    per-product run config YAML, runner token admin UI.

- id: P2-01+P2-02+P2-03
  date: 2026-05-06
  title: "Phase 2 — AI Pipeline: TestScriptGenSkill + Orchestrator + KB increments"
  outcome: >
    ai/skills/test_script_gen.py: TestScriptGenSkill converts test case .md docs to runnable
    pytest files. Domain-specific conventions (api/web/mobile/security) embedded as few-shot
    context. Strips markdown fences from model output automatically.
    ai/pipeline/orchestrator.py: TestPipelineOrchestrator runs full KB → doc → script pipeline.
    CLI: python -m ai.pipeline.orchestrator --feature <name> --domain <api|web|mobile|security>.
    Supports --increment for feature drops, --doc to skip doc gen.
    Updates data/kb_increments_log.yaml after each run.
    ai/prompts/prompt_templates.py: Added TEST_SCRIPT_GEN_PROMPT and FEATURE_DOC_PROMPT.

- id: P1-01
  date: 2026-05-06
  title: "Phase 1 — Persistent tracking system (data/)"
  outcome: >
    Created data/ with RESUME.md, architecture.md, conventions.md,
    progress/backlog.yaml, progress/completed.yaml, decisions/ADR-001, ADR-002.
    Any AI tool can now read RESUME.md and resume work without conversation context.

- id: P0-01
  date: 2026-05-06
  title: "Phase 0 — Stability fixes"
  outcome: >
    Removed hardcoded RP API key from pytest.ini (env var RP_API_KEY).
    Added MISTRAL_CLOUD_TIMEOUT_S to Mistral cloud requests.
    Added content-type guard in RestClient._validate_schema.
    Fixed lru_cache on instance methods in kb_loader.py (switched to _cache dict).
    Added locale fallback logging in LocatorManager.
    Extracted AI factory to utils/ai_factory.py.
    Created utils/constants.py for all magic numbers.
    BasePage uses LOCATOR_HEAL_TIMEOUT_MS constant.
