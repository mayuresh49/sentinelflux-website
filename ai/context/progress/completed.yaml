# Completed work items — append only, newest first

- id: explore-route-405-fix
  date: 2026-05-23
  title: "Fix: explore route 405 + increment product filter leak"
  outcome: >
    POST /{product}/explore and GET /explore-jobs/{job_id} were registered after
    GET /{product}/{filename} in kb.py — Starlette's catch-all two-param GET route
    captured both, returning 405 on the POST and 404 on job polling. Fixed by moving
    both routes before the /{product}/{filename} handlers. Also fixed increment filter
    in pages.py: removed the `and inc_product` guard so increments with no product:
    field are excluded (not leaked) when a product filter is active.

- id: pipeline-path-validation-hard-fail
  date: 2026-05-23
  title: "Fix: pipeline fails hard on hallucinated endpoint paths"
  outcome: >
    _validate_script_paths now checks endpoints[] from loaded increment YAMLs in addition
    to base api_specs.yaml paths. Suspicious paths raise ValueError instead of logging a
    warning — pipeline job marked failed with actionable error message rather than silently
    completing with broken tests.

- id: api-endpoint-hallucination-fix
  date: 2026-05-23
  title: "Fix: API endpoint hallucination prevention + performance_management doc+script rewrite"
  outcome: >
    get_increments_context() was silently dropping endpoints[] from increment YAMLs that don't
    use the test_scenarios schema — LLM got no endpoint data and fabricated paths. Fixed:
    kb_loader now emits "API CONSTRAINTS FOR <feature>" block with method+path+codes from
    endpoints[]. FEATURE_DOC_PROMPT gets rule 7 blocking path fabrication when constraints
    present. performance_management_requirements.md rewritten with 9 TCs (OH-API-029..037)
    using real paths. Script rewritten with orangehrm_client/orangehrm_api_base_url fixtures.

- id: explorer-kb-output
  date: 2026-05-23
  title: "Feat: AppExplorerAgent KB doc + increment generation from dashboard"
  outcome: >
    DiscoveredPage.to_kb_yaml_entry() produces ui_pages.yaml-compatible dicts;
    to_increment_entry() produces increment YAML for a single page.
    AppExplorerAgent.run() gains write_kb_doc (merges into products/<product>/ai/knowledge_base/ui_pages.yaml,
    replacing existing entries by URL) and write_increment (writes
    ai/knowledge_base/increments/explore_<feature>_<product>.yaml combining all pages).
    POST /api/kb/{product}/explore fires a background thread; GET /api/kb/explore-jobs/{job_id}
    polls in-memory status. Dashboard KB Increments tab adds "Explore App" panel with
    Base URL, auth fields, pages textarea, Feature Name, Output Mode select, polling spinner,
    and "Run Pipeline →" shortcut on completion.

- id: pipeline-feature-name-fix
  date: 2026-05-22
  title: "Fix: pipeline feature name collision + doc_gen/script_gen activity logging"
  outcome: >
    Feature name extraction used stem.split("_",2)[2] — collapsed all per-product YAMLs
    to the product name (e.g. "orangehrm"), overwriting the same output files on every
    DOCX upload. Fixed to strip "_<product>" suffix when --project matches, giving unique
    output files per feature (ess_requirements_orangehrm → ess_requirements).
    _generate_doc and _generate_script now log pending/success/error to ActivityLog
    with agent="doc_gen"/"script_gen" so the agents dashboard shows real status.
    CLAUDE.md updated with AI Pipeline rulebook section covering naming contract,
    agent logging, subprocess DB behavior, and SQLite diagnostics.

- id: gcp-deployment-scripts
  date: 2026-05-23
  title: "Chore: GCP e2-micro deployment scripts + private repo split"
  outcome: >
    gcp-setup.sh: GCP-specific bootstrap with swap, WeasyPrint libs, SSH deploy key
    setup, no Playwright (remote runner handles Playwright-based tests).
    Caddyfile.prod updated to app.sentinelflux.in. sentinelflux.service workers 2→1.
    deploy.sh uses GIT_SSH_COMMAND for private repo pulls.
    Private repo sentinelflux-app created and pushed. Public sentinelflux repo
    retains marketing site only.

- id: kb-product-scoped-ux
  date: 2026-05-22
  title: "Feat: product-scoped KB page + agent status logging + orphan pipeline job cleanup"
  outcome: >
    KB page blocks all upload/increment actions without product selection (toast + disabled).
    Increment list filtered by product: field in each YAML. YAML template pre-filled with
    selected product; $watch keeps it in sync. createIncrement, saveEditedDocxYaml, uploadDocx
    all pass product to API. DOCX endpoint injects product: field if LLM omits it.
    pages.py no longer auto-defaults selected_product to first product.
    orchestrator.py logs app_explorer, doc_review, script_review to ActivityLog so agents
    dashboard shows real status. app.py startup reaps orphaned pipeline_jobs in running state.
    ess_requirements_orangehrm.yaml patched with product: orangehrm.

- id: unit-tests-all-modules
  date: 2026-05-22
  title: "Feat: unit tests for all framework modules + fix apply_schema"
  outcome: >
    9 new test files, 232 new tests, all 256 tests pass.
    Modules covered: utils.assertions, utils.ai_assertions (hard+soft), utils.wait, utils.step;
    BugManager (CRUD, state machine, comments, artifacts, stats); InsightsManager + RoadmapManager;
    audit_logger; ai_factory (all provider/mode branches + dashboard config);
    RestClient (URL building, path params, schema validation); AgentContext + BaseAgent (domain helpers);
    KnowledgeBaseLoader (load/cache/invalidate, context formatters, increments);
    AnthropicClient + OpenAIClient + MistralClient (sys.modules mocking for optional packages).
    Also fixed core/db.py: apply_schema now ignores "no such table" on ALTER TABLE so fresh DBs
    work in tests; CREATE TABLE bugs now includes bug_seq + bug_number columns.

- id: website-github-pages
  date: 2026-05-20
  title: "Feat: sentinelflux.in marketing website + GitHub Pages deploy workflow"
  outcome: >
    website/index.html: static landing page — hero with fake dashboard preview,
    8 module cards (REST/GraphQL, Web UI, VAPT, Perf, A11y, API Contract, Visual,
    Bug Tracker), AI pipeline / self-healing / agents feature sections,
    how-it-works (3 steps), install CTA. website/CNAME: sentinelflux.in.
    .github/workflows/deploy-website.yml: peaceiris/actions-gh-pages deploys
    website/ to gh-pages branch on push to main. GoDaddy DNS: 4 A records to
    GitHub Pages IPs (185.199.108-111.153) + CNAME www → mayuresh49.github.io.

- id: nav-reorder-icon-audit
  date: 2026-05-20
  title: "Refactor: sidebar nav reorder + icon audit"
  outcome: >
    dashboard/templates/base.html: replaced nav_items loop + scattered conditional blocks
    with explicit grouped items and thin dividers. Order now: core workflow (Dashboard,
    Test Plans, Docs, Scripts, Runs, Quality, Failures) → specialized modules (Bugs, VAPT,
    Perf, A11y, Contract, Visual) → workflow (Assignments, Approvals, Activities) → AI
    (Agents, KB) → admin (Config, Users, Insights). Six icon fixes: Test Plans → calendar,
    Bugs → x-circle (was same triangle as Failures), API Contract → terminal, Activities →
    clock, Knowledge Base → open-book, Agents → sparkle.

- id: result-analyzer-db-fix
  date: 2026-05-20
  title: "Fix: result analyzer output never written to DB + SF_ENV forwarding + browser timeout"
  outcome: >
    sentinel_orchestrator.py: expose failure_analysis (by_classification dict + per-failure
    records) in run_post_suite summary. runs.py _run_post_suite: isinstance(blockers, dict)
    was always False — failure_categories never written; now reads failure_analysis correctly.
    _execute_run: SF_ENV forwarded as --env to pytest so product env_*.yaml is selected.
    orangehrm/env_qa.yaml: Playwright timeout 12s → 30s for demo-site latency.
    Root cause of run_65f104b4 all-errors: logged_in_page fixture timeout on Fill username
    (demo site load + 12s too tight).

- id: pipeline-spa-hardening
  date: 2026-05-19
  title: "Fix: AppExplorerAgent robustness for SPA apps + run_pipeline.sh explore flags"
  outcome: >
    Three fixes to AppExplorationSkill: (1) timeout_ms 15s→30s for SPA headroom,
    (2) fixed 1s wait replaced with _wait_for_spa() (networkidle best-effort + 500ms settle),
    (3) login verification — warns if still on login page after submit; networkidle guarded
    with 3s fallback. run_pipeline.sh gains SF_EXPLORE / SF_BASE_URL / SF_LOGIN_URL /
    SF_EXPLORE_PAGES env-var flags. Unblocks full DocGen→DocReview→Explore→ScriptGen→ScriptReview
    pipeline for OrangeHRM ESS module.

- id: bugs-transition-parity
  date: 2026-05-19
  title: "Fix: Bug Workflow matrix ↔ Move To dropdown parity"
  outcome: >
    _load_product_transitions now intersects default targets with product state names,
    matching _get_transitions behaviour. Both Config > Bug Workflow matrix and the
    Move-To dropdown are driven by identical logic. Fixes case where `resolved` appeared
    in matrix/dropdown for products that have no resolved status.

- id: bugs-export-dropdown
  date: 2026-05-19
  title: "Feat: Bug export dropdown — CSV/Excel from bugs page"
  outcome: >
    Export dropdown (download icon + chevron) added to the Bugs left-panel header
    next to "New Bug". exportBugs(fmt) reads active product/state/priority filters
    from Alpine and hits GET /api/bugs/export. Matches Test Case Docs export UX.

- id: runs-page-500-fix
  date: 2026-05-19
  title: "Fix: /runs page 500 crash — failure_categories list guard"
  outcome: >
    run_9d157fc3 had failure_categories stored as [] (list) instead of dict.
    Template now uses `is mapping` guard falling back to {}. _run_post_suite
    only writes failure_categories when blockers is a dict.

- id: bugs-export-endpoint
  date: 2026-05-19
  title: "Feat: Bug export — CSV/Excel via GET /api/bugs/export"
  outcome: >
    Streams all visible bugs with 23 columns (linked IDs, timestamps, metadata).
    format=csv (default) or format=xlsx (openpyxl). Product/state/priority filters supported.

- id: config-products-ux-redesign
  date: 2026-05-19
  title: "UX: Config > Products screen — two-row layout with module toggle chips"
  outcome: >
    Replaced clumsy single-row layout (6 toggle buttons + inline bug code form +
    actions all wrapping) with two-row card. Row 1: name/badges + bug code + danger
    actions. Row 2: module chips (colored=enabled, gray=disabled, tooltip=action).
    Removes ambiguous "VAPT On/Off" labels.

- id: bugs-transition-fallback-product-statuses
  date: 2026-05-19
  title: "Fix: align transition fallback with product's configured statuses"
  outcome: >
    _get_transitions now intersects _DEFAULT_TRANSITIONS with the product's
    actual status names when bug_transitions is unconfigured. Ensures Move To
    dropdown matches Bug Workflow matrix exactly — resolved excluded if not in
    product statuses; custom states get empty sets until admin configures them.

- id: bugs-run-artifact-attach-transition-fix
  date: 2026-05-19
  title: "Bugs: auto-attach run artifacts on creation; fix transition config fallback"
  outcome: >
    create_bug_from_run now calls _attach_run_artifacts — copies screenshot,
    console log, API log, trace.zip and JSON run report into bug artifact storage.
    _get_transitions: if raw is not None (was if raw) so empty bug_transitions:{}
    is honoured rather than falling back to defaults.

- id: ui-padding-standardisation
  date: 2026-05-19
  title: "UI: standardise padding/spacing across all dashboard pages"
  outcome: >
    All right/detail panels: border-slate-100 + shadow-sm. Section headers: px-5 py-3.
    Tab content: p-6. Config page: removed max-w-5xl constraint + title block, tightened
    mt/space-y. Covers 15+ templates and 2 partials.

- id: bugs-sequential-numbers
  date: 2026-05-19
  title: "Bugs: sequential bug numbers with per-product code (BUG-XX-001)"
  outcome: >
    bug_seq + bug_number columns in DB. _get_bug_code() reads per-product code from config.
    Inline bug_code editor in Config > Products. PDF report shows bug_number over UUID.

- id: bugs-id-display-plan-link
  date: 2026-05-19
  title: "Bugs: Bug ID display + test plan link in Details panel"
  outcome: >
    Details tab shows Bug ID row at top. Linked info row includes test plan
    link (/test-plans?highlight=) and updated empty-state label. Config/label/
    priority panels tightened to py-3 padding.

- id: activities-pagination-oob-fix
  date: 2026-05-19
  title: "Fix: activities OOB pagination triple-render on initial page load"
  outcome: >
    Gated OOB pagination blocks in activities_rows.html with HX-Request header
    check. Raw full-page renders no longer eject the divs from tbody into the
    visible page; OOB swaps fire only during HTMX requests.

- id: test-plan-report-open-bugs
  date: 2026-05-19
  title: "Test plan report: open bugs section with product-aware closed-state detection"
  outcome: >
    BugManager.list_bugs() gains linked_plan_id filter. _DEFAULT_STATUSES now
    carries is_open flag per entry. New closed_state_names(product) method reads
    is_open from configured statuses (custom statuses default to open). Report
    renderer uses closed_state_names instead of hardcoded set; passes open_bugs
    to template. PDF/HTML report renders an Open Bugs table between run history
    and scope sections.

- id: activities-top-pagination
  date: 2026-05-19
  title: "Activities: top pagination with HTMX OOB sync"
  outcome: >
    Added activities-pagination-top div above the table in activities.html.
    activities_rows.html partial now emits two hx-swap-oob blocks so both
    top and bottom pagination controls update together on every HTMX swap.

- id: bugs-custom-statuses-workflow
  date: 2026-05-19
  title: "Bugs: custom statuses per product with configurable workflow"
  outcome: >
    Per-product bug_statuses in config.yaml (name/label/color). _get_statuses()
    falls back to 7 default states. Config > Bug Workflow: Statuses section
    (CRUD with color picker) + Transitions matrix (uses actual product statuses).
    bugs.html: filter dropdown server-rendered; JS color maps data-driven via
    _COLOR_PALETTE + _statusMeta(); stateLabel() replaces all replace('_',' ').

- id: bugs-configurable-transitions
  date: 2026-05-19
  title: "Bugs: configurable per-product state transitions"
  outcome: >
    BugManager reads bug_transitions from config.yaml per product (falls back
    to defaults). New GET /api/bugs/{id}/transitions endpoint. loadTransitions()
    in bugs.html calls the API instead of hardcoded JS map. Config > Bug Workflow
    tab: checkbox matrix to configure allowed transitions per product, with Save
    and Reset to Defaults actions stored in config.yaml.

- id: bugs-transition-dropdown-update-button
  date: 2026-05-19
  title: "Bugs: transition dropdown + explicit Update Bug button"
  outcome: >
    Replaced flat transition buttons with a <select> dropdown. Removed @blur
    auto-save from textarea/text-input fields; added Update Bug button that
    PATCHes all text fields in one call. Select fields still auto-save on @change.

- id: vapt-ssh-multi-auth
  date: 2026-05-19
  title: "VAPT SSH multi-auth: key path, key paste, password"
  outcome: >
    Scope gains ssh_auth_method + ssh_key_content + ssh_password. UI shows
    the matching credential input per radio selection. Runner injects the
    right env vars. Conftest handles all three auth paths; key_paste writes
    a chmod-600 temp file, connects, deletes immediately. Pre-flight checks
    the correct field per method. Generator template updated.

- id: vapt-ssh-section-visibility
  date: 2026-05-19
  title: "VAPT scope: SSH credentials section always visible"
  outcome: >
    x-show gated on scopeForm.test_types.includes('infra_int') but infra_int
    is not in Test Domains checkboxes so section was permanently hidden.
    Removed guard — always visible, matching Infrastructure Targets pattern.

- id: vapt-infra-int-preflight
  date: 2026-05-19
  title: "VAPT infra_int: pre-flight SSH credentials check"
  outcome: >
    Missing ssh_username/ssh_key_path in scope caused 7 silent skips with a
    misleading 'completed' scan. Pre-flight check now fails the scan immediately
    with a human-readable summary_error before the subprocess is spawned.

- id: vapt-findings-scan-type-filter
  date: 2026-05-19
  title: "VAPT findings tab: filter by scan type"
  outcome: >
    filteredFindings, uniqueOwaspRefs, hasOpenFindings, and report-tab severity
    summary all read from the full findings array regardless of scanTypeSel.
    Promoted scanTypeFindings() method to a reactive Alpine getter scoped to
    current scan type scan IDs; wired into all consumers. Empty-state check
    and N-of-M counter also corrected.

- id: V-01
  date: 2026-05-19
  title: "VAPT: Infrastructure (Internal) — grey-box SSH scan"
  outcome: >
    New infra_int scan type alongside existing external infra. Test suite:
    vapt_infra_int/ with 7 SSH hardening checks via paramiko factory fixture
    (sshd_config: PermitRootLogin/PasswordAuthentication/MaxAuthTries/AllowUsers,
    SUID/SGID files, sensitive file permissions, audit log presence). Scope model
    adds ssh_username + ssh_key_path (key-path only, no plaintext creds). Runner
    injects VAPT_SSH_USER / VAPT_SSH_KEY_PATH env vars to pytest subprocess.
    Generator adds _INFRA_INT_CONFTEST + _INFRA_INT_SSH_CONFIG_TESTS templates.
    Dashboard: Internal Infra button in scan type selector, SSH credentials
    section in scope tab (conditionally shown when infra_int in test_types,
    finalize-lockable). 8 files changed, 3 new files.

- id: vapt-findings-test-data
  date: 2026-05-19
  title: "Data: VAPT reportportal engagement morphed with realistic findings"
  outcome: >
    eng-25d78fab7eae.json injected with 5 findings (Critical/High/Medium/Low ×
    open/still_open/accepted_risk/fixed) to exercise findings flow in dashboard UI.
    Infra scan: 0→3 failed, 8→5 passed. Web scan: 0→2 failed, 30→28 passed.
    Three test_log entries updated confirmed_secure→finding. Full description,
    evidence, and remediation text on each finding.

- id: vapt-infra-bug-fixes
  date: 2026-05-19
  title: "VAPT infra scan: xfail fix, real AXFR test, multi-target parametrize"
  outcome: >
    pytest.xfail → pytest.fail for exposed-ports and SSH-on-22 tests (security
    findings must be FAILED, not xfail). Zone transfer test now sends actual raw
    DNS-over-TCP AXFR query (struct+socket) and reads RCODE+ancount — passes when
    AXFR is blocked, fails only when transfer succeeds. conftest.py replaced
    vapt_infra_targets + single vapt_host session fixtures with _resolve_targets()
    + pytest_generate_tests so all vapt_host tests run once per IP. Same fixes
    applied to vapt_test_generator.py templates. Backlog item V-01 added for
    authenticated SSH grey-box scanning.

- id: agents-public-registry
  date: 2026-05-19
  title: "Agents: filter product-domain agents from public registry"
  outcome: >
    _PUBLIC_REGISTRY added to agents.py — excludes agents with domain=="product"
    (the 4 Insights persona agents). Public API endpoints (/api/agents list/get)
    and the home + agents dashboard pages now use _PUBLIC_REGISTRY so internal
    product-review agents are not surfaced externally.

- id: product-filter-retention
  date: 2026-05-19
  title: "Fix: product filter retention across all nav pages; suppress on Insights"
  outcome: >
    VAPT, Performance, Accessibility, API Contract, and Visual Regression sidebar
    nav links now carry ?product= when a product is selected. The main nav_items
    loop already did this; these five special-access pages were hardcoded hrefs.
    insights.html gets an empty header_filter block to suppress the global product
    filter on Product Insights (matches admin_users.html pattern).

- id: visual-compare-modal
  date: 2026-05-19
  title: "Visual Regression: baseline vs current screenshot compare modal + empty-state alignment"
  outcome: >
    New GET /api/visual/engagement/{eng_id}/screenshot endpoint serves stored PNGs from disk
    (type=baseline or type=scan&scan_id=). Template adds a lightbox modal (two-column Baseline|Current
    grid) with diff % badge; each per-page row in completed comparison scans gets a "View" button.
    Also fixed "No … suites yet" empty-state cards in a11y, contract, perf, visual to match the
    styled "Select a product" card (bg-slate-50 border rounded-xl p-6).

- id: startup-orphan-run-reaper
  date: 2026-05-19
  title: "Startup: auto-error orphaned queued/running runs on server restart"
  outcome: >
    _reap_orphaned_runs() added to FastAPI startup event in app.py. Marks any
    run in queued or running status as errored (finished_at=now, errors=1) at
    startup. Prevents runs submitted as background tasks from staying stuck
    indefinitely after a server restart (tasks are lost; runs would never
    complete without this). Users can retrigger from the Test Plans UI.

- id: cto-agent-strategic-roadmap
  date: 2026-05-19
  title: "Insights: CTO agent + Strategic Roadmap + deduplication fix"
  outcome: >
    CTOAgent reads all active insights from 4 expert agents, selects top 4-5
    strategically with LLM rationale. New roadmap_items SQLite table (source_insight_id
    UNIQUE). RoadmapManager: create/list/update_status/delete+restore. New
    /api/insights/roadmap CRUD + /roadmap/run trigger. Strategic Roadmap panel on
    /insights page: planned cards with persona badge, CTO Rationale block (indigo),
    Mark Done + Remove actions, collapsible Completed section. save_insights() now
    deletes existing active rows before inserting — re-runs replace rather than
    accumulate. CTO reads only active insights so planned/done sources are excluded.

- id: runs-latest-first-and-nav-link
  date: 2026-05-19
  title: "Fix: runs latest-first ordering + test plan → run navigation link"
  outcome: >
    Removed reversed() from runs_page and failures_page — all_runs() already
    returns DESC; reversed() was making the /runs page show oldest first.
    Added ?highlight=<run_id> to runs_page: finds the run's position in the
    filtered list, jumps to the correct page, renders an indigo ring on the card,
    and scrolls to it via JS scrollIntoView. Run cards have id="run-{id}" anchors.
    Run IDs in Test Plans → Execution → Automated Run History are now links:
    open /runs?product=...&highlight=<id>#run-<id> in a new tab. Client sort
    switched from localeCompare to direct > comparison (locale-safe for ISO dates).

- id: insights-error-surfacing
  date: 2026-05-19
  title: "Insights: surface run errors in UI instead of silently dropping them"
  outcome: >
    _run_agent_task wrapped in try/except; sets in-memory _run_state dict to
    {state: error, error: msg} on any failure (no client, empty insights, exception).
    /api/insights/runs returns state dict alongside runs timestamps.
    Polling detects state=error immediately and shows error toast.
    Error banner added below run-control bar showing last failure message.
    Already-running guard added to trigger endpoint.

- id: product-insights-module
  date: 2026-05-19
  title: "Product Insights: AI expert review from 4 perspectives (master-admin only)"
  outcome: >
    /insights page (master-admin gated) with 4 AI review agents: ProductManagerAgent,
    DevArchitectAgent, QAArchitectAgent, UXArchitectAgent. Each reads relevant codebase
    context (RESUME.md, db.py, conftest.py, base.html, backlog.yaml) and calls the LLM
    to produce 5–8 structured insights (title, description, recommendation, category,
    priority). New product_insights SQLite table with id, agent_type, title, description,
    recommendation, category, priority, status, run_id, run_at, updated_at columns.
    InsightsManager CRUD in core/insights_manager.py. API router at /api/insights/ with
    GET /, GET /runs, POST /run/{agent_type}, PATCH /{insight_id}, DELETE /agent/{agent_type}.
    UI: persona tab selector with active-count badges, run-control bar with last-run
    timestamp, status filter chips (active/planned/punted/discarded), 2-col card grid
    with expandable descriptions, inline status transitions (Plan/Punt/Restore/Discard),
    polling every 3 s until run_at changes. All 4 agents registered in agents/__init__.py,
    _AGENT_REGISTRY (agents.py), and AGENT_META (agent_meta.py). Nav item in base.html
    gated by master_admin flag.

- id: test-plans-report-export
  date: 2026-05-18
  title: "Test Plans: execution report (PDF/HTML export) + latest runs on top"
  outcome: >
    GET /{plan_id}/report?format=pdf|html endpoint added to test_plans router.
    test_plan_report_pdf.html template: cover page (plan name, product, owner,
    status, generated_at), plan overview table with milestones, progress metric
    cards (total/pass/fail/blocked/pass_rate) with stacked colour bar, TC
    execution status table grouped by domain with colour-coded badges, automated
    run history table, scope summary. _to_pdf() helper using WeasyPrint.
    "Preview Report" (opens in new tab) and "Export PDF" (download) buttons
    added to the Execution tab. Linked runs sorted latest-first in both API
    (sorted by triggered_at desc) and client (localeCompare sort after fetch).

- id: nav-access-flags-config-page
  date: 2026-05-18
  title: "Fix: perf/a11y/contract/visual nav items invisible on /config page"
  outcome: >
    config/__init__.py config_page() built its own context dict that only set
    vapt_access; the 4 new module flags were undefined (falsy), hiding nav items
    when the user navigated to /config. Fixed with a local _has_flag(flag) helper
    covering all 5 flags. Nav items now consistent across all pages.

- id: test-plans-empty-modules-ux
  date: 2026-05-18
  title: "Test Plans: empty-modules empty state UX fix and API error surfacing"
  outcome: >
    Empty state in Scope tab now shows the expected file path
    (products/{product}/tests/{domain}/) and test_*.py pattern so users know
    where to add scripts. loadAvailableModules now triggers sfToast on API
    failure (!r.ok or network error) instead of silently leaving the list blank.
    Added scopeLoadError flag to distinguish API failure from legitimately empty
    directory. scopeLoadError reset on plan change and at start of each load.

- id: config-module-feature-flags
  date: 2026-05-18
  title: "Config: Perf/A11y/Contract/Visual feature flag toggles in product settings"
  outcome: >
    Added On/Off toggle buttons for all 4 new modules in Configurations → Products.
    Backend: _set_flag() helper + 4 POST endpoints (set-perf, set-a11y, set-contract,
    set-visual) writing to config.yaml. Template: coloured pill buttons matching VAPT
    toggle pattern (blue/sky/emerald/pink). Fixed missing module enable path that caused
    "No X-enabled products" message on all 4 new module pages.

- id: four-new-testing-modules
  date: 2026-05-18
  title: "4 new modules: Performance Testing, Accessibility Testing, API Contract Validation, Visual Regression"
  outcome: >
    All 4 modules follow VAPT pattern: FileLock JSON storage, FastAPI router,
    Alpine.js dashboard (modals + polling), WeasyPrint PDF report, nav items in sidebar.
    Feature flags per product config. _module_access() helper in pages.py.
    Performance: httpx+asyncio load engine; profiles (load/stress/spike/soak);
    VUs/duration/ramp-up/endpoints; p50/p75/p95/p99/throughput/error-rate metrics;
    configurable thresholds; PDF report. Accessibility: Playwright + axe-core CDN;
    WCAG 2.1 A/AA/AAA; per-page violations by impact (critical/serious/moderate/minor);
    multi-scan history; PDF report. API Contract: OpenAPI spec from URL or file path;
    validates status codes + jsonschema per endpoint; skips required path-param routes;
    PDF report. Visual Regression: Playwright full-page screenshots; Pillow pixel diff
    via ImageChops.difference; configurable threshold %; baseline management per page;
    comparison scan shows diff_pct per page; PDF report. Pillow>=10.0.0 added to
    requirements.txt. Files: core/perf_manager.py, core/a11y_manager.py,
    core/contract_manager.py, core/visual_manager.py; 4 routers; 4 main HTML templates;
    4 PDF report templates. 20 total dashboard pages/routers.

- id: vapt-apk-static-analysis
  date: 2026-05-18
  title: "VAPT: APK static analysis + infra/mobile scope inputs + scan delete + per-type reports/certs"
  outcome: >
    Per-scan-type reports/certs: check_certifiable/issue_certificate filter by scan_type;
    certs in eng["certificates"][scan_type]; download endpoints accept scan_type param;
    scan type toggle moved above sub-tabs. Scan delete: VaptManager.delete_scan() +
    DELETE endpoint; admin Delete button on scan cards. Infra targets: scope.infra_targets[],
    textarea in scope form, VAPT_INFRA_TARGETS env var to subprocess; conftest
    vapt_infra_targets fixture. Mobile app path: scope.mobile_app_path, VAPT_MOBILE_APP_PATH
    env var; vapt_mobile_app_path fixture (Path|None). APK static analysis: 2 new generated
    files — test_vapt_mobile_apk_manifest.py (androguard: debuggable, allowBackup,
    usesCleartextTraffic, exported components, taskAffinity) and test_vapt_mobile_apk_static.py
    (pure Python: private keys, hardcoded secrets/http URLs in assets, weak crypto in DEX;
    androguard: DEX string pool secrets, cert pinning, Log calls). androguard>=3.3.5 added
    to requirements.txt. All APK tests skip gracefully without APK or androguard.

- id: docs-tc-create-delete-ux
  date: 2026-05-18
  title: "Docs: TC create success toast + sfConfirm delete + Alpine timing fix"
  outcome: >
    tc_create redirect appends ?created=TC_ID; docsPage.init() shows sfToast
    then cleans param via history.replaceState. Delete inline confirm replaced
    with sfConfirm modal + hidden x-ref form triggered via htmx.trigger.
    tcCreateForm moved to docs.html block scripts (page-load scope) — fixes
    Alpine MutationObserver race where function was undefined on first HTMX swap.

- id: coverage-gap-pipeline-orangehrm
  date: 2026-05-18
  title: "Coverage gap pipeline: orangehrm 67% → 100% doc coverage + pipeline hardening"
  outcome: >
    CoverageGapAgent run across api/web/mobile: 96 untested KB scenarios logged to activity.
    Full pipeline (DocGen→DocReview→ScriptGen→ScriptReview) for 4 undocumented scripts:
    security_api.md (OH-API-029..039), security_web.md (OH-WEB-083..125),
    leave.md (OH-WEB-124..128), login_mobile.md (OH-MOB-017..026).
    Pipeline bugs fixed: normalizer regex now excludes not_automated + not_automatable;
    max_tokens 3000→5000; mobile CRITICAL — no base_url in mobile POM constructors.
    Credential hardcoding eliminated: api convention rewritten with {product}_client /
    {product}_api_base_url / {product}_credentials patterns; two NEVER rules added to
    prompt_templates.py. orangehrm_ess_credentials fixture + env_qa.yaml ess keys added.

- id: docs-manual-tc-creation
  date: 2026-05-18
  title: "Docs: manual test case creation from the dashboard"
  outcome: >
    Add Test Case button on Docs page loads a form (HTMX → #tc-viewer).
    TC ID auto-generated from highest existing ID in product+domain.
    Feature dropdown shows existing modules; "Add new module" reveals text input.
    Script restricted to actual test_*.py files; warns if already linked elsewhere.
    Owner dropdown from config.yaml users. On save: appends to existing .md or
    creates new file with full header. Duplicate ID rejected. HX-Redirect refreshes
    left panel. Fixed Alpine | safe bug (raw JSON in double-quoted attribute) and
    moved <script> to top of partial (before MutationObserver fires).

- id: quality-vapt-exclusion-coverage-signal
  date: 2026-05-18
  title: "Quality metrics: exclude VAPT tests; low-coverage doc-gen signal"
  outcome: >
    quality.py: _EXCLUDED_DOMAINS = {"vapt"} applied in _script_features(),
    _scripts_by_domain(), _count_test_functions() — VAPT template-generated tests
    no longer inflate script count or drag down doc coverage.
    _undocumented_with_domain(product) returns [{feature, domain}] for scripts
    lacking a matching test case doc. per-product data gains undocumented_scripts list.
    quality.html: health signals section adds low-coverage card per product when
    doc_coverage < 70% — collapsible list of undocumented scripts, each with a
    Generate Doc button (POST /api/pipeline/trigger, skip_script=true, Alpine state
    machine idle→pending→done/error, sfToast feedback).

- id: tc-doc-section-spacing
  date: 2026-05-18
  title: "TC doc section spacing: blank lines before Pre-conditions/Steps/Expected Result"
  outcome: >
    prompt_templates.py: web + API format examples now have blank lines between every section.
    orchestrator.py _clean_doc(): _TC_HEADERS regex (pattern: **<keyword>[^*]*\*\*) ensures
    blank line before each bold section header on every pipeline run; also strips inline ```
    fences. 23 existing docs reformatted in one pass. Bug caught: colon is inside bold
    markers (**Pre-conditions:**), not after — fixed regex accordingly.

- id: vapt-exhaustive-suite-template-viewer
  date: 2026-05-18
  title: "VAPT: exhaustive A01-A10 test suite, template viewer, dedup product filter"
  outcome: >
    core/vapt_test_generator.py: 9 test files / 31 tests. New: test_vapt_injection
    (A03 — SQL/XSS/path traversal/SSTI), test_vapt_session (A07 — cookie flags),
    test_vapt_web (A01/A05 — clickjacking/open redirect/referrer/HTML comments),
    test_vapt_ssrf (A10 — IMDS/RFC-1918 probes). OWASP inference: clickjack A03→A05,
    new keywords path_traversal/httponly/samesite/dns_rebinding. vapt.py: test-templates
    endpoint. vapt.html: collapsible template viewer (file tabs + OWASP context + code).
    pages.py: vapt_page() calls vapt._vapt_products() — no more duplicated filter logic.
    RP: 25 passed, 3 skipped, 3 xfailed.

- id: script-fn-id-normalizer
  date: 2026-05-18
  title: "Orchestrator: _normalize_script_fn_ids() — code-level TC ID backstop for scripts"
  outcome: >
    orchestrator.py: _normalize_script_fn_ids(script_path, doc_path, tc_prefix) runs after
    _generate_script() and before _review_script(). Extracts TC IDs from doc index table
    (non-not_automatable rows), matches to test function declarations in order, renames any
    function with wrong embedded ID (test_OH_API_001_x → test_OH_API_014_x) or no ID prefix.
    Skips silently when count mismatch (needs human review). Mirrors _normalize_tc_ids() for docs.

- id: quality-dashboard-metrics
  date: 2026-05-18
  title: "Quality dashboard: real pass rate, automated test count, execution stats, risk score"
  outcome: >
    quality.py: pass rate now from test_runs table (actual pytest results), not activity_log.
    Added _count_test_functions() (def test_ grep per product), _run_stats_from_db() (aggregates
    total/passed/failed/duration), _daily_pass_rate_from_runs() (7-day daily trend from test_runs),
    _risk_score() (composite 0–100: pass gap 50pt + quarantine rate 20pt + doc gap 20pt + flaky 10pt).
    quality.html: expanded from 5 to 6 summary cards (Automated Tests, Pass Rate, Executions,
    Doc Coverage, Quarantined, Risk Score). Each card has Alpine ⓘ tooltip with formula.
    Per-product table: auto test function count, real pass rate, execution breakdown (passed✓/failed✗),
    risk badge (Low/Med/High + score). Live data: 169 test functions, 44% pass rate, 191 executions,
    risk 43 (Medium).

- id: timesheets-pipeline-fixes
  date: 2026-05-18
  title: "Timesheets pipeline: ScriptReview KeyError fix, orchestrator fixes, correct test scripts"
  outcome: >
    script_review_agent.py: escaped {{"positive","negative","edge"}} in _REWRITE_PROMPT — str.format()
    was raising KeyError silently, causing all ScriptReview passes to be skipped.
    orchestrator.py: DocReview now always runs with --doc flag; --doc path resolved absolute;
    default doc-model changed to qwen2.5-coder:14b-instruct-q4_K_M.
    tests/api/test_Timesheets.py: rewritten OH-API-014..028 with real assertions and orangehrm_client.
    tests/web/test_Timesheets.py: completed all 11 TCs OH-WEB-072..082.

- id: vapt-standard-test-suite
  date: 2026-05-18
  title: "VAPT standard test suite — per-product generation on enable"
  outcome: >
    core/vapt_test_generator.py: VaptTestGenerator generates products/<product>/tests/vapt/
    with conftest.py (auto-detects base_url/api_token from env_*.yaml) and 5 test files:
    test_vapt_auth (A07), test_vapt_headers (A05), test_vapt_access_control (A01),
    test_vapt_crypto (A02), test_vapt_rate_limiting (A04). 17 tests total.
    Rate-limiting tests use xfail (advisory), HTTPS tests use skip for local envs.
    _products.py: auto-generates on first VAPT enable. vapt.py: test-info + generate-tests
    endpoints. vapt.html: test suite status card + Regenerate button in Scans tab.
    reportportal vapt suite: 13 passed, 2 skipped, 2 xfailed against live RP instance.

- id: vapt-security-review-fixes
  date: 2026-05-18
  title: "VAPT security review: autoescape HTML reports + redundant auth dependency"
  outcome: >
    dashboard/routers/vapt.py: _render_template() now uses jinja2 Environment with
    autoescape=True and FileSystemLoader — prevents XSS from test-failure evidence strings
    in the HTML report preview endpoint. issue_certificate consolidated to single
    Depends(_require_admin) since _require_admin already wraps require_user.

- id: doc-gen-robustness-vapt-certifiability
  date: 2026-05-18
  title: "Doc generation: strip code fences + TC_ID placeholder handling; VAPT certifiability fix"
  outcome: >
    orchestrator.py: _clean_doc() strips ``` code-fence wrappers models add around output
    before normalization runs. _normalize_tc_ids() now replaces literal TC_ID placeholders
    (models copying prompt template verbatim) in addition to renumbering PREFIX-NNN IDs.
    vapt_manager.py: check_certifiable() now requires at least one completed scan
    (was checking for any findings, which could be satisfied without a completed scan run).
    ReportPortal security scan: 5/5 auth tests passed against live RP instance.

- id: tc-id-generation-hardening
  date: 2026-05-17
  title: "TC ID generation hardening — normalizer, stronger prompt, increments string guard"
  outcome: >
    orchestrator.py: _normalize_tc_ids() post-processes every generated doc,
    remapping wrong IDs to the correct sequential range starting at tc_start.
    test_case_doc_kb.py: _build_tc_id_instruction() now shows a concrete
    4-step example sequence and "NEVER start from PREFIX-001" rule.
    kb_loader.py: get_increments_context() handles string 'feature' keys
    (was crashing and skipping DocReview silently on every pipeline run).

- id: doc-id-conflict-fix
  date: 2026-05-17
  title: "TC ID conflict prevention — rulebook, pipeline auto-detection, doc fixes, new docs"
  outcome: >
    conventions.md: added TC ID Rules section with prefix table, 5 uniqueness rules,
    current highest ID snapshot. orchestrator.py: _find_highest_tc_id() auto-detects
    tc_start from existing docs; main() uses it to prevent silent ID collisions.
    test_case_doc_kb.py: CRITICAL warning added to _build_tc_id_instruction() prompt.
    15 existing docs: manual test IDs renumbered away from automated ID ranges
    (OH-WEB 058-071, RB-WEB 012-014). 9 new docs created for undocumented test files
    (security, accessibility, mobile for OH and RB). DocReviewAgent run on all 15 docs.

- id: P2-04+P2-05
  date: 2026-05-17
  title: "ScriptReviewAgent + AI assertion utilities"
  outcome: >
    ai/agents/script_review_agent.py: Post-generation quality gate for pytest scripts.
    Static AST pass detects hardcoded URLs/credentials, time.sleep(), duplicate test
    names, POM base_url issues. LLM rewrite pass fixes weak/missing assertions, missing
    domain markers, missing status-code checks on API tests, and exact-equality checks
    on AI output. Integrated into TestPipelineOrchestrator after _generate_script().
    Registered in __init__, _AGENT_REGISTRY, AGENT_META.
    utils/ai_assertions.py: Hard assertion helpers for non-deterministic AI output —
    assert_confidence_above/below, assert_category_in, assert_text_contains_any/all,
    assert_text_similarity (difflib), assert_response_within, assert_field_present.
    SoftAssertions context manager collects all failures into one combined AssertionError.
    Thresholds (AI_CONFIDENCE_THRESHOLD=0.70, AI_TEXT_SIMILARITY_RATIO=0.75,
    AI_RESPONSE_TIMEOUT_S=30.0) centralised in utils/constants.py.

- id: SQLite-migration+Hetzner+DocReviewAgent
  date: 2026-05-16
  title: "SQLite storage migration, Hetzner deploy, DocReviewAgent, Analyze All"
  outcome: >
    Storage: replaced FileLock+JSON/YAML with SQLite WAL mode (data/sentinelflux.db)
    via core/db.py. All shared state (runs, schedules, approvals, pipeline jobs,
    activity log, quarantine, run history) now in DB. Superseded D-01/D-02.
    pipeline_jobs migrated to SQLite (separate pass, 6e25c71).
    Hetzner: server setup and deployment scripts added (4510522).
    DocReviewAgent (b94dfa3): post-generation quality gate for test case docs.
    Detects batched TC headings, missing mandatory sections, thin TCs (<3 steps).
    Rewrites failing TCs via LLM. Auto-runs after DocGenAgent. Visible in /agents.
    DocGenAgent prompt hardening: mandatory Fields-in-Scope rule, no range headings,
    min-3-steps rule. max_tokens raised (web 3000→6000, API 3000→4000).
    Analyze All bulk endpoint + Re-analyze button for runs page (d8f431e).

- id: D-01-through-D-06+multi-tenant+remote-runner
  date: 2026-05-15
  title: "Debt D-01..D-06 resolution + multi-tenancy + remote runner"
  outcome: >
    D-01: FileLock added to all data writers (later superseded by SQLite migration).
    D-02: QuarantineManager.record_run() trims entries older than 90 days.
    D-03: utils/paths.py created; ROOT imported across all modules.
    D-04: config_router.py split into dashboard/routers/config/ subpackage
    (_helpers, _meta, _users, _products, _assignments, _run_config, _runners).
    D-05: Unit tests added to tests/unit/ for core utilities.
    D-06: _build_ai_client() extended to support openai/anthropic providers.
    Multi-tenancy: per-product run/schedule sharding → later migrated to SQLite.
    Remote runner: pull-based daemon (sentinelflux runner), Bearer token auth,
    per-product run config YAML, runner token admin UI.

- id: P2-01+P2-02+P2-03
  date: 2026-05-06
  title: "Phase 2 — AI Pipeline: TestScriptGenSkill + Orchestrator + KB increments"
  outcome: >
    ai/skills/test_script_gen.py: TestScriptGenSkill converts test case .md docs to runnable
    pytest files. Domain-specific conventions (api/web/mobile/security) embedded as few-shot
    context. Strips markdown fences from model output automatically.
    ai/pipeline/orchestrator.py: TestPipelineOrchestrator runs full KB → doc → script pipeline.
    CLI: python -m ai.pipeline.orchestrator --feature <name> --domain <api|web|mobile|security>.
    Supports --increment for feature drops, --doc to skip doc gen.
    Updates data/kb_increments_log.yaml after each run.
    ai/prompts/prompt_templates.py: Added TEST_SCRIPT_GEN_PROMPT and FEATURE_DOC_PROMPT.

- id: P1-01
  date: 2026-05-06
  title: "Phase 1 — Persistent tracking system (data/)"
  outcome: >
    Created data/ with RESUME.md, architecture.md, conventions.md,
    progress/backlog.yaml, progress/completed.yaml, decisions/ADR-001, ADR-002.
    Any AI tool can now read RESUME.md and resume work without conversation context.

- id: P0-01
  date: 2026-05-06
  title: "Phase 0 — Stability fixes"
  outcome: >
    Removed hardcoded RP API key from pytest.ini (env var RP_API_KEY).
    Added MISTRAL_CLOUD_TIMEOUT_S to Mistral cloud requests.
    Added content-type guard in RestClient._validate_schema.
    Fixed lru_cache on instance methods in kb_loader.py (switched to _cache dict).
    Added locale fallback logging in LocatorManager.
    Extracted AI factory to utils/ai_factory.py.
    Created utils/constants.py for all magic numbers.
    BasePage uses LOCATOR_HEAL_TIMEOUT_MS constant.
